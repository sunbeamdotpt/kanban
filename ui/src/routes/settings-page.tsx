/**
 * SettingsPage — board settings and configuration.
 * TODO(Stage 6h): Replace with actual board settings form.
 */

export function SettingsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold mb-4">Board settings</h1>
      <p className="text-muted">Settings form will render here.</p>
    </div>
  );
}
